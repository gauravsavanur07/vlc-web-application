import leafName from "@/utils/leaf-name";
import { ADD_MEDIA } from "./mutation-types";

const initialState = {

};

const getters = {

};

const actions = {

};

const mutations = {
  [leafName(ADD_MEDIA)](state: object, payload: object) {
    return state;
  },
};

export default {
  actions,
  getters,
  mutations,
  namespaced: true,
  state: initialState,
};
